using TMPro;

namespace Timers
{
    public class TimerMinutesAndSecondsView : TimerView
    {
        // protected override void PrintTime(TMP_Text text, double timeLeft)
        // {
        //     double minutes = timeLeft / 60;
        //     double seconds = timeLeft % 60;
        //     text.text = $"{minutes:F0}:{seconds:00}";
        // }
    }
}