using ScriptBoy.Digable2DTerrain;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FragmentationGranade : Weapon
{
    //[SerializeField] private FragmentationGranadesPool _fragmentsPool;

    //protected override void InitializeProjectile(Projectile projectile, ExplosionPool explosionPool, Shovel shovel)
    //{
    //    var fragmentationGranadeProjectile = (FragmentationGranadeProjectile)projectile;
    //    fragmentationGranadeProjectile.Init(explosionPool, shovel, _fragmentsPool);
    //}
}
